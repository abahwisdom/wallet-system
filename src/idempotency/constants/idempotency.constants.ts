export const IDEMPOTENCY_KEY_PREFIX = 'idempotency:';
export const IDEMPOTENCY_LOCK_PREFIX = 'idempotency:lock:';
export const DEFAULT_IDEMPOTENCY_TTL = 300; // seconds
export const DEFAULT_LOCK_TTL = 10; // seconds
